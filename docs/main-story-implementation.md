# Main story and opening journey: implementation guide

**Implementation status (September 2026):** the first three main-story chapters are playable in the current build: Orro and the shrine, Pella's cave component, and Talla's three-branch grid. Chapters four and five at the Measured Oasis remain planned. The landing page provides a five-moment walkthrough with those statuses and a horizontal gallery of current-build screenshots in `screenshots/`. Instructions below describe both the completed foundation and the remaining arc; use the current story catalog as the source of truth for live mission IDs.

## Theme, character arc, and travel rhythm

The plot asks where tested knowledge ends and ritualized numerical mysticism begins, and how people use both without confusing symbols for measurements or reducing a living community to a ledger. Show this twice: in the **microcosm**, a person must decide how to repair and interpret one light; in the **macrocosm**, a town and then a network must decide how to distribute, record, and keep those lights. Neither reason nor ritual is the villain. Reason checks repeatable claims and the costs of action; ritual holds attention, relationship, and the unanswered question. Both can harden into avoidance. A ritual can insist on a beautiful but false sequence; a ledger can hide an unpaid human cost. End the first complete arc with repeatable evidence still recorded and a modest shared rite that keeps it open to revision.

Characters embody choices rather than teach slogans. **Orro** can measure the first pulse but needs to trust another person's touch; **Marn** uses accounting as care, then notices the households beyond the entry; **Pella** names those people but respects the winter stores; **Talla** manages all three branches together rather than privileging her favorite; **Varo** protects reproducibility without demanding certainty about every experience; **Nemi** offers symbolic attention and submits its predictions to checking; **Saima** makes the final public practice answerable to both. Give every pivotal character a concrete caring motive and a specific limitation; make their pending and completed lines respond to visible world changes.

For pacing, annotate each mission leg with `newArea: true | false` (computed from discovered chunk/region or landmark IDs), `defaultPath`, `storyScale: "person" | "town" | "network"`, and its next destination. A leg to a **new** area counts as **mission + exploration**; returning to a known area advances the **mission** only; optional branches and places discovered beyond the suggested route count as **exploration**, with a side or mini quest if they yield an authored task. Use the player's actual discovery save for display, because a cave is no longer new on a second visit. These are pacing labels, not extra player-facing homework. Default choices and the default compass marker should move the story toward its next objective. Non-default dialogue choices may reveal a person, a detour, or a site, and must have a graceful way back. Never require exhaustive branch exploration for the next main chapter.

| Moment | Scale and use of reason / ritual | Default activity | New area on a first visit | Optional exploration |
| --- | --- | --- | --- | --- |
| Orro and the shrine | Person: observe one measured pulse; pause and attune to it | Talk → activate → tell Marn | Shrine and Wammigmig: mission + exploration | Ask Orro how the shrine keeps a pattern; inspect roadside space |
| Pella's missing component | Person to household: recover a physical part; listen to whose dark room matters | Cave → retrieve → return/use | Cave: mission + exploration; return to Wammigmig: mission | Cave side branches and other objects, with known exits retained while inside |
| Talla's three branches | Town: verify that all grid branches conduct; acknowledge distinct users | Travel to major town → turn all on → confirm | Major town: mission + exploration; confirmation walk: mission | Speak to households, exchange workers, and labyrinth witnesses |
| Varo and Nemi | Town to network: test quantities; let symbolism propose a question to test | Approach capital → inspect local totals → record uncertainty | Capital approach: mission + exploration; repeated tests: mission | Compare interpretations and visit a side-found shrine or reading |
| Saima's synthesis | Network: test a named sequence; create a public rite that can be corrected by new evidence | Complete late sequence → agree on shared practice | New capital precinct: mission + exploration; return to Saima: mission | Visit additional districts and cultural arcs; return to the main marker when ready |

Treat this as a **complete vertical-slice story arc** from person to network, even if implementation ships in stages. The first act must have a small payoff, the middle must show distinct towns and their consequences, and the capital must change the shared grid and its social practice. Count first-visit destinations and revisits per chapter in the compiler report; playtest the alternation so there is no long chain of reports or backtracking without a fresh place, character, consequence, or optional discovery. Once the player has visited a place, the mission remains meaningful through changed dialogue or world state rather than pretending the journey is new again.

### Main landing page as a story preview

Keep `index.html` distinct from `play.html`. Below the opening links and existing location shortcuts, show a five-step, keyboard/touch-accessible plot walkthrough: a short premise, one selected chapter at a time, place/character, personal or civic stakes, what the player does, the first-visit exploration label, and a screenshot with truthful caption and alt text. Next/previous buttons and chapter selectors change only the preview; they do not modify game saves or spoil the exact puzzle answers. Include **Play from the beginning** and direct **Visit this place** links using only validated `play.html?location=...` destinations; explain that a location link is a preview shortcut preserving progress, while a new game begins the story normally. Mark unimplemented narrative chapters as **planned** even when their location and existing grid can already be visited. Do not call a current screenshot proof of a future quest. Use local screenshots and no new startup asset requests from the playable world pack; keep page useful without JavaScript and on a phone.

## Selected cast and geography (seed0/v2)

Use these existing IDs for the first authored arc. Names for the unnamed regional NPC and the cave item below are **new proposed authored names**; the places, portals, and existing named characters are already present in the baked world. Keep the IDs and roles stable across dialogue rewrites.

| Stop | Stable anchor and approximate position `(x, z)` | Cast and dramatic purpose | Built content / required addition |
| --- | --- | --- | --- |
| Spawn and roadside shrine | Spawn `(800, 907)`; `shrine:shrine 19 22:782:903` `(784, 905)` | **Orro Coil-Tender** (`actor:wammigmig:orro`), the anxious, affectionate technician, introduces one trustworthy act of repair. | Orro currently stands `(787.5, 904.5)` beside the actual shrine. Automatic chapter begins with Orro as the first indicated conversation, then shrine activation; Marn confirms in town. |
| Small starting town: **Wammigmig** | `town:populationBoarmanTribe#381` `(820, 900)` | **Marn Even-Tally** (`legacy-actor:BoarmanTribe#381:544`) keeps the accounts and confirms the road light; **Pella Sharekeeper** (`actor:wammigmig:pella`) represents homes that the account misses. | Move the existing three readings and `puzzle:wammigmig:distribution` out of chapter one; let Pella introduce the missing cave part and eventually receive or fit it. |
| The nearby southern cave | `cave:760:920:0` entrance `(786.5, 928.5)`; baked component size 406 | **Pella** gives the personal reason to retrieve one component; **Orro** can interpret it on return. No new required cave NPC. | Bake one stable item instance, tentatively **the listening coil**, in a reachable chest inside this cave; anchor a fitting near Pella. This is a new placement. The existing optional CopperCoil chest is on the surface and does not satisfy the cave chapter. |
| First bigger town: **Wamwammigtraawsho** | `town:populationBoarmanTribe#423` `(860, 940)`; Wammigmig's actual `parentSecondaryCapitalTownId` | Give existing `actor:boarman:regional:1` a proper authored name, **Talla Three-Wicks**, a circuit keeper who explains how exchange, homes, and the labyrinth share a supply. Keep her existing actor ID. | Reuse `puzzle:boarman:regional:1`: the existing **Exchange / Homes / Labyrinth** three-switch puzzle already has `mechanic: "all"`. Provide an authored return conversation and route cue northward. This Boarman major town is distinct from Awshowam, its distant Boarman capital. |
| Central Antipion capital: **Migbushoprahshotra** / the Measured Oasis | `town:populationAnatipionCatchers#390` `(780, 1100)` | **Saima of Nine Measures** (`actor:antipion:principal:0`) receives the main story; **Varo Null-Scribe** (`actor:antipion:principal:1`) checks reproducible totals; **Nemi Reed-Breath** (`actor:antipion:principal:2`) notices what the record cannot yet name. | Introduce subset sums on the approach or outskirts with Varo, then reserve a clearly explained named sequence for the late capital chapter with Saima and Nemi. `puzzle:antipion:capital` is currently an **all-switch** puzzle; author a separate late sequence or migrate its semantics explicitly rather than silently treating it as one. |

The first stop to cave entrance is about 50 world units as the crow flies; cave entrance to Wamwammigtraawsho about 77; the major town to the Measured Oasis about 179. Wammigmig to the major town is about 57. These are **planning geometry only**; all walking budgets, interior legs, and shrine connections must use navigable paths and portal transitions. The southern cave requires a short outward detour before the major-town road. If route validation finds it unreasonably long, keep the same cave entrance and rearrange the handoff or waypoint spacing before changing the chosen locale.

### Named handoffs and optional threads

1. **Orro → shrine → Marn:** Orro speaks first, one light wakes, Marn receives news of its pulse. The first chapter ends on confirmation with Marn. Pella then points to the missing coil as the next main chapter; she is sympathetic rather than a second source of tutorial instructions.
2. **Pella → cave → fitting → Talla:** The retrieved cave coil changes a visible socket at Wammigmig and makes the road toward Wamwammigtraawsho legible. Talla receives the practical observation and asks the player to wake all three local grid branches. Orro remains available for optional technical intuition about the coil.
3. **Talla → Saima / Varo / Nemi:** Talla's completed circuit gives a measured account to carry north. Saima asks what the account misses; Varo helps test quantities before any sequence puzzle; Nemi offers an alternative reading and a clue to the late ordered activation. Saima handles the capital synthesis and the next main-story handoff.
4. **Side and mini quests:** Marn's storage/household readings become a local side quest that reframes Talla's three branches and returns the marker to her or the current main chapter. Orro can give one short relay-inspection mini quest; Pella may retain a separate household service task. Varo and Nemi can offer optional observation threads that return to Saima. Do not make any of these prerequisites for the main chapter, and do not duplicate the cave coil in the old surface-chest side quest.

### Longer term expansion around the story spine

The five chapters are the **central thread**, not the intended total amount of content. Build multiple linked missions around each chapter, using most reachable settlements, resident actors, shrines, buildings, caves, labyrinths, and cultural capitals over time. Extend the same pattern to the Human forum at Trawamprahshoaw (Mara, Oren, Iris), Boarman capital Awshowam (Garrum, Bela, Tusk), Yeti hearth (Aro, Ketu, Seli), Kobold Deep Ledger (Vek, Nix, Rul), and Greek Horde labyrinth (Iris-of-the-Turn, Ember-Tusk, Clasp-of-Seven). These places expose different consequences of the same grid and the five layers; they do not all need to be required stops on the first linear route.

Give each available resident a small authored role before adding missions mechanically: a local need, an observation or belief, a nearby actionable site, and a connection to the current chapter or another resident. Some remain witnesses or provide Intuition; many offer side quests; only a small cast drives main chapters. Attach optional arcs to a named main chapter and provide a return marker or specific onward handoff on most completions. Generate coverage reports per region (placed actors, actors with dialogue, quest givers, quest sites, unreachable/unused anchors) so expansion genuinely reaches most of the existing world without filling the journal with generic repeated jobs. Keep new authored personalities explicitly distinct from recovered legacy identities and settlement names.


This section is the implementation brief for the next revision of the opening progression. It supersedes the **mechanical order** of the earlier “First playable exploration slice” and the current compiled `A Fair Share of Light` opening; retain those sections as character, theme, and geography references. The current compiler puts shrine, three evidence readings, a weighted sum, and a commitment into one first mission. The current browser route activates the shrine before meeting the giver; meeting Marn first creates a long return journey. Break that activity into linked chapters, so a new player always has one clear next promise.

### Story spine and quest taxonomy

Define one authored, versioned **main story** manifest with ID `story:concordance:opening`, title, premise, ordered chapter IDs, chapter entry and exit conditions, and a next-chapter fallback. The premise: a practical repair at Wammigmig exposes a failure in the Concordance Grid; a missing component in a nearby cave reveals that the problem extends toward the mystical Measured Oasis; competing ways of knowing must be reconciled on the journey to the central Antipion capital. This is an authored plot overlay, not a claim that the legacy save contained missions. Identify the opening's central capital explicitly as **Migbushoprahshotra / the Measured Oasis**; **Awshowam** remains the Boarman cultural capital for its later economic arc. Do not silently conflate a chapter's major town, a culture's capital, and the eventual central destination.

Every mission has `kind: "main" | "side" | "mini"`, `storyId` and `chapterId` where applicable, stable `id`, `requires`, giver or automatic start, objective IDs, turn-in rule, reward, and optional `followUpMissionIds` and `returnToMainMissionId`. A **main quest** is a chapter of the story spine and is always present in the journal once reached; a **side quest** develops a local character or consequence and can return attention to a main objective; a **mini quest** is a short, nearby activity (one target or compact cluster) that can be completed while exploring. A mini quest may belong to a side quest without becoming a main chapter. Keep authored story relationships in a separate story catalog or small manifest; chunk scenes carry only anchors and IDs, and dialogue/mission JSON remains keyed and streamed.

Start chapter one automatically on a new game, without requiring an NPC acceptance choice. Show its next goal immediately on the map/compass; the NPC conversation supplies character and stakes. Advance the story on an objective or report transaction exactly once, even across reloads. An automatic chapter should never require pressing an invisible Accept button; optional dialogue choices may affect tone or journal preference without blocking progression. Save only mission/chapter state and player navigation choices, never duplicated prose or scene content.

| Chapter | Place and activity | Player learns | Completion and handoff |
| --- | --- | --- | --- |
| 1. The road remembers | Starting shrine / Orro, then Wammigmig / Marn: **talk → activate one shrine → confirm**. Order the actors and shrine so the player does not have to double back. If Orro is the first talk, Marn is the confirmation; alternatively use Marn as both giver and nearby confirmer after moving the target. | Talk, Action, light feedback, journal, one marker | Marn explains why the cave component matters; automatically surface chapter 2 and its first waypoint. No evidence readings or switch arithmetic yet. |
| 2. A missing current | **Pella → `cave:760:920:0` → Pella's fitting**: enter, follow the listening-coil marker, take **one** marked component, use the exit markers, return and fit it. | Travel, portal, cave exploration, chest/item, return or use | Installing it reveals the next settlement relay and introduces the people affected. Do not consume the item before the use/report state is persisted. |
| 3. Every branch | **Wamwammigtraawsho / Talla Three-Wicks**: activate **Exchange, Homes, and Labyrinth** using the existing `puzzle:boarman:regional:1` all-switch circuit, observe the town respond, and confirm with Talla. | Multiple independent toggles and shared power | Reveal the road frontier north toward the Measured Oasis; optional readings explain different interpretations of the repair. |
| 4. What the numbers mean | **Varo Null-Scribe**, on the approach to the Measured Oasis or its outskirts: present a **subset/total** puzzle with Intuition showing each value, current total, and goal. Make evidence visits purposeful and local. | Selecting some branches and comparing quantities | Establish why correct arithmetic alone may leave a social cost. The existing Wammigmig sum circuit can instead become Marn's optional side quest. |
| 5. An order worth keeping | **Saima and Nemi at Migbushoprahshotra**: near the end, after all earlier patterns are familiar, use sequence switches with a named ordering and an in-world explanation of the binding; resolve with Saima. | Sequence, interpretation, synthesis | Persist chapter outcome, change visible grid state and dialogue, and expose the wider cultural arcs. |

This table specifies **difficulty and narrative order**, not a requirement to implement every chapter immediately. Implement and test chapters 1 and 2 first, then the all-switch chapter; keep later chapter stubs/manifest links so developers can follow the plot without shipping unreachable markers or promising nonfunctional content. The first shrine, cave, and item/fitting need verified reachable anchors and real portal mapping. Reuse Pella's existing coil/chest/socket content only if the item really lies in the chosen cave; the current nearby chest must not be presented as cave exploration. Preserve the existing sum puzzle as later authored material; keep sequence puzzles gated until the end. Neither the opening mission nor its journal text should describe all these mechanics at once.

### Travel budget and wayfinding

Measure **walkable path distance**, counting cave corridors and return legs; straight-line distance is insufficient around buildings and walls. In the current build, spawn → Marn → shrine → the three readings → two switches → Marn is approximately **209 one-unit walkable steps**; the test-script order that touches the shrine before talking is roughly **151 steps**. These are grid path estimates, not measured seconds of play. They are a baseline to improve, not target distances for the replacement.

For each proposed chapter emit a compiler report with leg ID, start/end realm and anchor, path length in cells, portal transition, estimated walking time using actual controller speed, and cumulative steps since last dialogue/reward. Validate player reachability and marker availability at **every** stage. Design target for the first chapter: a visible first contact, one short journey to a shrine, and a nearby confirmation, with no cross-town round trip; use playtesting to tune concrete limits. The cave chapter may be longer because exploration is its lesson, but place a light/reward or character beat before and after the cave. Each town-to-town leg needs its own forward shrine frontier and clear arrival cue. On cave entry show known mission goals and all discovered exits; on exit hide cave-only exit markers, as already required by the map system. If a goal is unknown, guide to the last known entrance or informant rather than revealing the hidden target.

### Side quests, mini quests, and journal attention

Journal top level: **Main story** (chapter, current objective, next destination, progress through named chapters), **Side quests** (town/character, state), **Mini quests** (nearby and short), with completed entries collapsed. Mark every entry with its kind in text and icon, not color alone. Keep the main story visible at the top even while a side quest is tracked. New game defaults navigation to the first main objective. Selecting another quest changes only the explicit navigation selection; returning from a side quest should offer a one-tap **Continue main story** action and restore the main waypoint when there is no newer explicit selection. Preserve the player's deliberate choice to track a side quest until they finish it or change it; never silently overwrite a manually selected target mid-quest.

Link a side quest to the main story with `returnToMainMissionId` or a chapter relationship and an authored handoff line: the local result changes or clarifies a coming main decision, marks the main destination, then invites the player to resume. Aim for a return to the main marker after most side quest completions, especially at travel transitions; allow chains that deliberately hand off to another nearby side quest or mini quest. Side quests may supply insight, Lightmarks, shortcuts, or visible improvements, but required main progression must not rely on optional quests. A mini quest should usually point back to its parent side quest or current main chapter on completion. Show nearby mini quests below the main objective, limit automatic marker clutter, and distinguish **available** offers from **accepted** commitments. The map/compass should prioritize the selected objective, show the main story as a distinct secondary marker when relevant, and avoid dumping every mission target at once.

### Data and runtime integration for agents

1. Add a compact `stories` catalog / keyed story record to the interaction manifest, including chapter IDs, titles, synopsis, successor mapping, and chapter-to-mission IDs; explicitly validate the entire graph for missing IDs, cycles, and unreachable chapter starts. Keep dialogue and mission records individually retrievable or partitioned by town/chapter so late capital text is not fetched at spawn; only chapter summary/navigation metadata belongs in the small global manifest. Keep assets and scene anchor IDs stable for the three.js editor.
2. Extend mission compiler helpers in `scripts/compile_interactions.mjs` with explicit `kind`, `storyId`, `chapterId`, `startMode: "automatic" | "offer"`, `followUpMissionIds`, and `returnToMainMissionId`. Generate an ordered chapter map from authored data rather than inferring narrative order from array position, NPC proximity, culture, or `requires` alone. Move the current `START_MISSION` contents into small, explicitly linked definitions; update outdated wording in the earlier slice document as work lands.
3. Extend `validateContent` in `jCRPG-engine/js/interactions/format.js` to validate taxonomy, all cross-references, each chapter's entry/exit and successor, cave item reachability and solvability, and the rule that an optional side/mini quest cannot block the main spine. Maintain support for an optional objective with more than one target when the all-switch lesson needs it.
4. Extend `InteractionRuntime` initialization and completion transactions: materialize the first automatic main mission on new/old saves when appropriate; unlock the next chapter exactly once; grant objective credit from previously observed world events when `backfill` is enabled; expose `currentStoryChapter`, `mainNavigationGoal`, and selected-quest goal separately. Completion must be idempotent for shrine, item, fitting, turn-in, and rewards. Dialogue reads current chapter/mission state, not a hard-coded start mission ID.
5. Update `InteractionUI.openJournal`, quest detail, compass, and map to render the three sections and a persistent main-story header. Remove the current special case for `mission:wammigmig:fair-share` and the present grouping trick that treats a tracked quest as `active` regardless of its true state. Use accessible touch targets and short next-step labels on phone; Action/Intuition remain the two touch verbs. Do not show a cave exit marker after leaving the cave.
6. Define a **migration map** from existing saved mission IDs/objective IDs to the new chapters. Preserve activated shrines, collected items, installed items, Lightmarks, and completed puzzles. Map completed `fair-share` to all already-satisfied early chapters without replaying rewards; map partial progress from world deltas, and show the first unmet chapter. Do not mark cave retrieval complete merely because the old nearby coil was collected. Version/migrate interaction content deliberately; do not reset the campaign by changing an ID.
7. Validate three paths in browser: fresh start and chapter 1; chapter 2 with cave entry, known item marker, auto exit markers and safe return; old save with shrine activated or `fair-share` complete. Check journal ordering, manual side tracking, a side/mini return to main, reload at every transition, and compiler travel report. Stop the initial implementation at the all-switch milestone; reserve later sum and sequence content for subsequent build steps.

**Editing rule:** mission IDs identify persistent player progress and must not change just because dialogue, captions, objectives' prose, rewards, or anchor positions are revised. Treat objective IDs and completion semantics as save contracts too. When a semantic change is unavoidable, write an explicit versioned migration and a before/after progression test. Keep story synopsis, chapter link, objective definition, and NPC dialogue as separate authored fields; a writer can rewrite one mission or conversation without breaking `requires`, navigation, or the next chapter. Build validation should print the story graph and its main/side/mini links in a readable report for developers.
